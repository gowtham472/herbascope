# HerbaScope X runtime + training dependencies (Python 3.12). Pinned for reproducibility.
# Linux/Docker: install torch from the CPU wheel index first to avoid the multi-GB CUDA
# build:  pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu

# Vision encoder
torch==2.14.0
transformers==5.17.0
safetensors==0.8.0

# Image handling and quality signals
pillow==12.3.0
opencv-python-headless==5.0.0.93
numpy==2.5.3

# Dataset preparation (DIMPSAR ships as parquet)
pandas==3.0.5
pyarrow==25.0.1

# Classifier, calibration and retrieval
scikit-learn==1.9.1
joblib==1.6.0
faiss-cpu==1.15.0

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0
