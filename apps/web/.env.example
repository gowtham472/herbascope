# HerbaScope X web app configuration. Copy to `apps/web/.env.local`.
# Base URL of the FastAPI service, including the /api/v1 prefix. Used by the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
